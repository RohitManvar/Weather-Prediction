# Weather Prediction (Rule-based + ML)

This project predicts:
- Temperature (Regression)
- Rain (Classification)
- Weather Condition (Rule-based logic)

## How to Run
1. Install requirements
   pip install -r requirements.txt

2. Train models
   python train_models.py

3. Predict weather
   python predict.py
